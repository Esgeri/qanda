h4 Attachments:
.attachments
  = f.fields_for :attachments do |a|
    = render 'common/attachment_field', f: a
    .links
      = link_to_add_association 'Show attachment form', f, :attachments, partial: 'common/attachment_field'
